multiuser on
acladd mlloreda

escape ^Zz

autodetach            on
hardcopydir           /tmp/
startup_message       off
vbell                 off             # default: ???
defscrollback         7500            # default: 100
silencewait           15              # default: 30
defshell bash

hardstatus alwayslastline
hardstatus string '%{=}%-w%{.r}%n %t%{-}%+w %=%{g} %H %{y} %d %M %C%a'
term xterm

# prestart
#screen -t emacs 0 em -t
#screen -t mutt 1 mutt
#screen -t slrn 3 slrn
#screen -t top 2 top -s3

# I su a lot
# bind R screen -t root su -

# screen-saver-ish stuff
# blankerprg cmatrix -s
# idle 15


bind K quit
